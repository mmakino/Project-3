import React from 'react';
import ReactDOM from 'react-dom';
import Button from '@material-ui/core/Button';
 
const Login = (props) => {
  return (
    <Button className="LoginComponent" variant="contained" color="primary">
      Hello World
    </Button>
  );
}
 
export default LoginComponent;